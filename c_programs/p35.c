#include<stdio.h>
#include<stdlib.h>
#include<conio.h>

int main(int argCount, char *args[])
{
    int termValue = atoi(args[1]);
    int numberOfTerms = 0;
    printf("N value = %d, Number of terms=%d\n",termValue)
}